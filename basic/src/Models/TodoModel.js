//A common description one of used type of data used in application

class TodoModel {
    constructor(id, text, timestamp) {
        this.id = id;
        this.text = text;
        this.timestamp = timestamp;
    }
}

export default TodoModel;